
import common from './common';

export default {
  ...common,
  props: {
    checked: false,
    size: 'default',
    disabled: false,
    loading: false,
    autofocus: false
  },
  mock: {
    
  }
}